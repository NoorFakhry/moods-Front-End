import React, {Fragment} from 'react';
import NavBar from '../NavBar/NavBar';

const HomePage = () => {
    return (
        <Fragment>
        </Fragment>
    )
};

export default HomePage;